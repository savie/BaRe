<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".SwiftApp"
        android:allowBackup="false"
        android:label="Swift Backup — Reference"
        android:supportsRtl="true"
        android:theme="@style/Theme.BaRe">
        <activity android:name=".settings.SettingsActivity" />
        <activity android:name=".folders.FoldersActivity" />
        <activity android:name=".messagescalls.MessagesCallsActivity" />
        <activity android:name=".appslist.AppListActivity" />
        <activity android:name=".detail.DetailActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".intro.IntroActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
